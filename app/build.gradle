apply plugin: 'com.android.application'

android {
    compileSdkVersion 34
    defaultConfig {
        applicationId "com.masterisk_f.accesssupporter"
        minSdkVersion 24
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    namespace 'com.masterisk_f.accesssupporter'
    lintOptions {
        checkReleaseBuilds false
        abortOnError false
        disable 'OutdatedLibrary'
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    androidTestImplementation('com.android.support.test.espresso:espresso-core:2.2.2', {
        exclude group: 'com.android.support', module: 'support-annotations'
    })
    implementation 'com.android.support:appcompat-v7:24.2.1'
    implementation 'com.android.support:design:24.2.1'
    implementation 'com.android.support:support-v4:24.2.1'
    implementation 'com.google.android.gms:play-services-maps:10.2.4'
    testImplementation 'junit:junit:4.12'
}

//csvダウンロード
def f_sta = new File("app/src/main/assets/" + "station_database_out_" + "station.csv")
f_sta.delete()
f_sta << new URL("https://raw.githubusercontent.com/Seo-4d696b75/station_database/main/out/main/station.csv").openStream()
def f_line = new File("app/src/main/assets/" + "station_database_out_" + "line.csv")
f_line.delete()
f_line << new URL("https://raw.githubusercontent.com/Seo-4d696b75/station_database/main/out/main/line.csv").openStream()
def f_reg = new File("app/src/main/assets/" + "station_database_out_" + "register.csv")
f_reg.delete()
f_reg << new URL("https://raw.githubusercontent.com/Seo-4d696b75/station_database/main/out/main/register.csv").openStream()

